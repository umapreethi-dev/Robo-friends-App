import React from 'react';

const SearchBox = () => {
    return (
        <div>
            <input type="search" placeholder="search friends"></input>
        </div>
    );
}

export default SearchBox;